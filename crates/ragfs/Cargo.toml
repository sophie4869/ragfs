[package]
name = "ragfs"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "A FUSE filesystem for semantic search and RAG architectures"
keywords = ["rag", "semantic-search", "fuse", "filesystem", "embeddings"]
categories = ["command-line-utilities", "filesystem"]

[[bin]]
name = "ragfs"
path = "src/main.rs"

[features]
default = ["candle", "lancedb"]
full = ["candle", "lancedb", "vision", "mount"]
# FUSE mount support. Off by default so the CLI (index/query/status) builds
# on platforms without libfuse (e.g. macOS without macFUSE).
mount = ["dep:ragfs-fuse", "dep:fuser", "dep:daemonize"]
candle = [
    "ragfs-embed/candle",
    "ragfs-index/candle",
    "ragfs-query/candle",
    "ragfs-fuse?/candle",
]
lancedb = [
    "ragfs-store/lancedb",
    "ragfs-index/lancedb",
    "ragfs-query/lancedb",
    "ragfs-fuse?/lancedb",
]
vision = [
    "ragfs-extract/vision",
    "ragfs-index/vision",
    "ragfs-fuse?/vision",
]

[dependencies]
ragfs-core.workspace = true
ragfs-fuse = { workspace = true, optional = true }
ragfs-index.workspace = true
ragfs-store.workspace = true
ragfs-query.workspace = true
ragfs-embed.workspace = true
ragfs-extract.workspace = true
ragfs-chunker.workspace = true
tokio.workspace = true
axum.workspace = true
tower-http.workspace = true
clap.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
directories.workspace = true
anyhow.workspace = true
thiserror.workspace = true
fuser = { workspace = true, optional = true }
blake3.workspace = true
chrono.workspace = true
daemonize = { workspace = true, optional = true }

[dev-dependencies]
tempfile.workspace = true
async-trait.workspace = true
uuid.workspace = true
chrono.workspace = true
criterion.workspace = true
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }

[[bench]]
name = "embedding"
harness = false

[[bench]]
name = "search"
harness = false

[[bench]]
name = "indexing"
harness = false

[lints]
workspace = true
